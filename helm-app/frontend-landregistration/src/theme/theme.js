// ✅ src/theme/theme.js
'use client'; // allow hooks in case you use them later

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  // You can add your custom theme options here
});

export default theme;
